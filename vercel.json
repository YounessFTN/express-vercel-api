{
  "version": 2,
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "builds": [
    {
      "src": "index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [{ "src": "/api/(.*)", "dest": "/server.ts" }],
  "buildCommand": "npm install && npx prisma generate"
}
